default: "test"

test:
  project:
    submodule:
      client:
        key1: "Config.yaml: test.project.submodule.client.key1"
    clients:
      clientA: "config.yaml: clientA.key1"
      clientB: "config.yaml: clientB.key1"

production:
  doc: "Options for the production environment"
  project:
    doc: "Python module specific options"

flatfile:
  application:
    opt1: 'flat file option 1'
